import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "GridMind — Power Theft Detection Dashboard",
  description:
    "Real-time anomaly detection dashboard for smart meter power theft monitoring. AI for Bharat hackathon prototype by GridMind.",
  keywords: ["power theft", "anomaly detection", "smart meter", "edge AI", "GridMind"],
};

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body>{children}</body>
    </html>
  );
}
